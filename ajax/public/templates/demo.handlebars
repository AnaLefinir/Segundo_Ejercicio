{{#each this}}
    <div class="task task-{{id}}">

        <input type='checkbox' class="check-element" data-id={{id}} {{#if done}} checked="checked" {{/if}}>

        <h2 class="title-{{id}}">{{title}}</h2>
        <hr>
        <p>{{description}}</p>

        <div class="del" data-toggle="modal" data-target="#confirm-delete"  data-id={{id}}>✘</div>

    </div>
{{/each}}
